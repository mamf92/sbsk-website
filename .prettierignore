node_modules
dist
build

# Agent worktrees are separate checkouts with their own build and test output.
.claude/worktrees

# Test artifacts
test-results
playwright-report
blob-report
coverage

# The inline script's exact bytes are pinned by a CSP hash in the same file (see
# githubPagesRedirect404Html.test.ts) — reformatting it silently breaks that hash.
public/404.html
